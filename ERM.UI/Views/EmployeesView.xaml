<UserControl x:Class="ERM.UI.Views.EmployeesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- ШАПКА  -->
        <Grid Grid.Row="0" Margin="0,0,0,28">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="Сотрудники"
                           Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                           FontWeight="Bold"/>
                <TextBlock Text="Управление персоналом и ролями"
                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                           FontSize="13" Margin="0,2,0,0" Opacity="0.7"/>
            </StackPanel>

            <!-- Поиск -->
            <TextBox Grid.Column="1"
                     Width="260" VerticalAlignment="Center"
                     md:HintAssist.Hint="Поиск..."
                     md:TextFieldAssist.HasClearButton="True"
                     md:TextFieldAssist.LeadingIcon="Magnify"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     md:HintAssist.IsFloating="False"/>

            <!-- Кнопка добавления -->
            <Button Grid.Column="3"
                    Command="{Binding AddEmployeeCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Height="42" Padding="20,0"
                    md:ButtonAssist.CornerRadius="8">
                <StackPanel Orientation="Horizontal">
                    <md:PackIcon Kind="AccountPlusOutline" Width="18" Height="18"
                                 VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="Добавить" FontWeight="Medium" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Индикатор загрузки -->
        <ProgressBar Grid.Row="1" IsIndeterminate="True" Height="2" Margin="0,0,0,8"
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <!-- ТАБЛИЦА -->
        <md:Card Grid.Row="2" md:ElevationAssist.Elevation="Dp2" UniformCornerRadius="10">
            <DataGrid ItemsSource="{Binding Employees}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      HeadersVisibility="Column"
                      BorderThickness="0"
                      Background="Transparent"
                      RowHeight="52"
                      Style="{StaticResource MaterialDesignDataGrid}"
                      md:DataGridAssist.ColumnHeaderPadding="16,14"
                      md:DataGridAssist.CellPadding="16,0">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                        <Setter Property="FontSize" Value="11"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Opacity" Value="0.6"/>
                        <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBodyLight}"/>
                        <Setter Property="Background" Value="{DynamicResource MaterialDesignToolBarBackground}"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>

                    <!-- ФИО -->
                    <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="FontWeight" Value="Medium"/>
                                <Setter Property="FontSize" Value="14"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Телефон -->
                    <DataGridTextColumn Header="Телефон" Binding="{Binding PhoneNumber}" Width="160">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBodyLight}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Роль — бейдж -->
                    <DataGridTemplateColumn Header="Роль" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                                    <!-- Бейджи ролей -->
                                    <ItemsControl ItemsSource="{Binding ., 
                                  Converter={StaticResource EmployeeRolesConverter}}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <!-- Один шаблон на все роли -->
                                                <Border CornerRadius="6" Padding="10,4" Margin="0,0,4,0"
                                    Background="{Binding Background}">
                                                    <TextBlock Text="{Binding Label}"
                                           FontSize="12" FontWeight="Medium"
                                           Foreground="{Binding Foreground}"/>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <!-- Прочерк если ролей нет -->
                                    <TextBlock Text="—" FontSize="14" Opacity="0.35"
                           VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HasNoRole}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Примечания -->
                    <DataGridTextColumn Header="Примечания" Binding="{Binding Notes}" Width="1.5*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="FontSize" Value="13"/>
                                <Setter Property="Opacity" Value="0.75"/>
                                <Setter Property="ToolTipService.InitialShowDelay" Value="200"/>
                                <Setter Property="ToolTip">
                                    <Setter.Value>
                                        <ToolTip MaxWidth="300"
                                                 Background="{DynamicResource MaterialDesignCardBackground}"
                                                 BorderThickness="0"
                                                 md:ElevationAssist.Elevation="Dp3">
                                            <TextBlock Text="{Binding Notes}" TextWrapping="Wrap"
                                                       Foreground="{DynamicResource MaterialDesignBody}"
                                                       Padding="4" FontSize="13"/>
                                        </ToolTip>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Номер машины -->
                    <DataGridTextColumn Header="№ машины"
                    Binding="{Binding MachineNumber, TargetNullValue='—'}"
                    Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="FontSize" Value="14"/>
                                <!-- Прочерк серый, число — обычный цвет -->
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="—">
                                        <Setter Property="Opacity" Value="0.35"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Действия -->
                    <DataGridTemplateColumn Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center">
                                    <!-- Редактировать -->
                                    <Button Style="{StaticResource MaterialDesignToolButton}"
                                            Command="{Binding DataContext.EditEmployeeCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="Редактировать"
                                            Width="32" Height="32" Padding="0">
                                        <md:PackIcon Kind="PencilOutline"
                                                     Width="16" Height="16"
                                                     Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                    </Button>
                                    <!-- Удалить -->
                                    <Button Style="{StaticResource MaterialDesignToolButton}"
                                            Command="{Binding DataContext.DeleteEmployeeCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="Удалить"
                                            Width="32" Height="32" Padding="0"
                                            Margin="2,0,0,0">
                                        <md:PackIcon Kind="TrashCanOutline"
                                                     Width="16" Height="16"
                                                     Foreground="#E53935"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>
        </md:Card>
    </Grid>
</UserControl>
